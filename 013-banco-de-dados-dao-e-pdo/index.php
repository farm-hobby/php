<?php

    require_once('config.php');

    $user = new User();

    // $user->loadById(1);

    // echo json_encode(User::getList());

    // echo json_encode($user->search('user'));

    // $user->login('superuser', 'sudo123');

    // echo $user;

?>
